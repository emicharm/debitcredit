import { createAccount, getAccounts, getAccount, accountsQuery, updateAccountAsNew, archiveAccount } from '$lib/database/accounts';

// Re-export all account operations
export { createAccount, getAccounts, getAccount, accountsQuery, updateAccountAsNew, archiveAccount };
